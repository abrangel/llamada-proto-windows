name: Build Cluely Installer

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- PASO AÑADIDO ---
      # Forzamos el uso de Python 3.11, que es compatible con node-gyp
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          # Usar '20.x' es una mejor práctica para la versión LTS
          node-version: '20.x'
          cache: 'npm' # Añadido para acelerar futuras builds

      - name: Install dependencies
        run: npm install

      - name: Build Electron App
        run: |
          npm run build || echo "No build script"
          npx electron-builder --win --x64

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: Cluely-Installer
          # Asegúrate de que la ruta sea correcta, 'dist' es común para electron-builder
          path: dist/*.exe
